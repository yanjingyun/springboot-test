server:
  port: 8080
spring:
  #数据库连接配置
  datasource:
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///test?characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8

#mybatis的相关配置
mybatis:
  #mapper配置文件
#  mapper-locations: classpath:mapper/*.xml
#  type-aliases-package: com.yjy.entity
  #开启驼峰命名
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com:
      yjy:
        user:
          mapper: debug
